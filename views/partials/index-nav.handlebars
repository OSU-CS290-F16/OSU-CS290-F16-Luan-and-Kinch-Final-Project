<nav>
    <div class="menu-bar">
        <div class="drop-menu">
            <a href="/" class="redirect-item" name="menu-item"><b>Final Project</b></a>
        </div>
        <div class="drop-menu">
                <a href="#" class="redirect-item" name="about-item">About</a>
        </div>
        <div class="drop-menu">
            <a href="#" class="redirect-item" id="add-category-item">Add New Category</a>
        </div>

        <div class="registe-menu">
            {{#if userName}}
                <div class="drop-menu">
                    <a href="#" class="redirect-item">{{userName}} <i class="fa fa-caret-down"></i></a>
                    <div class="drop-menu-item">
                        <a href="/" class="redirect-item">Sign Out</a>
                    </div>
                </div>
            {{else}}
                <a href="#" class="redirect-item", name="login-btn">Sign In</a>
            {{/if}}
        </div>
    </div>
</nav>